#!/bin/sh
set -eu
ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ ! -x "$ROOT/.venv/bin/python" ]; then
  printf '%s\n' 'Install the Python environment first; see docs/quickstart.md.' >&2
  exit 1
fi
cd "$ROOT"
exec "$ROOT/.venv/bin/python" -m tools.studio_service "$@"
